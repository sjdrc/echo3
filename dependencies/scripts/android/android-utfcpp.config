#Skip utfcpp as it does not have any sources to compile, Just copy the headers over.
ONLY_SETUP=1
echo Copying contents of utfcpp/source to $ECHO_LIB_DIR/include/utf8
mkdir -p $ECHO_LIB_DIR/include/utf8
cp -R source/* $ECHO_LIB_DIR/include/utf8
